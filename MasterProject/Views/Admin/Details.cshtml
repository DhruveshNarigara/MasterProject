
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Details</title>
</head>
<body>
    <h2>Employee Details</h2>

    <div>
        <h4>Employee</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">Employee ID</dt>
            <dd class="col-sm-10">@Model.c_empid</dd>
            
            <dt class="col-sm-2">Employee Name</dt>
            <dd class="col-sm-10">@Model.c_empname</dd>

            <dt class="col-sm-2">Gender</dt>
            <dd class="col-sm-10">@Model.c_empgender</dd>

            <dt class="col-sm-2">Date of Birth</dt>
            <dd class="col-sm-10">@Model.c_empdob</dd>

            <dt class="col-sm-2">Shift</dt>
            <dd class="col-sm-10">@Model.c_empshift</dd>

            <dt class="col-sm-2">Department</dt>
            <dd class="col-sm-10">@Model.c_empdept</dd>

            <dt class="col-sm-2">Image</dt>
            <dd class="col-sm-10">
                <img src="~/images/@Model.c_empimagePath" class="rounded-square" height="100" width="100" />
            </dd>
        </dl>
    </div>

    <div>
        <a href="@Url.Action("Index", "Employee")" class="btn btn-success">Back to Employee List</a>
    </div>
</body>
</html>
